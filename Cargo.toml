[package]
name = "hypersolve"
version = "0.1.0"
edition = "2024"
readme = "README.md"

[dependencies]
hyperreal = { path = "../hyperreal" }
hyperlattice = { path = "../hyperlattice", default-features = false }

[dev-dependencies]
proptest = "1.7.0"
